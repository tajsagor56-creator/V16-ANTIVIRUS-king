name: V16 Antivirus APK

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Build APK
    runs-on: ubuntu-22.04

    env:
      ANDROID_HOME: /usr/local/lib/android/sdk
      ANDROID_SDK_ROOT: /usr/local/lib/android/sdk
      ANDROID_NDK_HOME: /usr/local/lib/android/sdk/ndk/27.3.13750724
      ANDROID_NDK_ROOT: /usr/local/lib/android/sdk/ndk/27.3.13750724
      ANDROID_NDK: /usr/local/lib/android/sdk/ndk/27.3.13750724

    steps:

      # =====================================================
      # 1. Checkout
      # =====================================================
      - name: Checkout
        uses: actions/checkout@v4


      # =====================================================
      # 2. Python 3.11
      # =====================================================
      - name: Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"


      # =====================================================
      # 3. Java 17
      # =====================================================
      - name: Java 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"


      # =====================================================
      # 4. Verify Python + Java
      # =====================================================
      - name: Verify Python and Java
        shell: bash
        run: |
          set -eux

          echo "=========================================="
          echo "PYTHON"
          echo "=========================================="

          python --version
          which python

          echo
          echo "=========================================="
          echo "JAVA"
          echo "=========================================="

          java -version

          echo
          echo "JAVA_HOME:"
          echo "$JAVA_HOME"

          if ! java -version 2>&1 | grep -q '"17'; then
            echo "ERROR: Java 17 active হয়নি."
            exit 1
          fi

          echo
          echo "Java 17 OK."


      # =====================================================
      # 5. Install Buildozer
      # =====================================================
      - name: Install Buildozer
        shell: bash
        run: |
          set -eux

          echo "=========================================="
          echo "INSTALLING BUILDOZER"
          echo "=========================================="

          python -m pip install --upgrade pip

          python -m pip install \
            "buildozer==1.5.0" \
            "cython<3" \
            "virtualenv"

          echo
          echo "Installed packages:"
          python -m pip list


      # =====================================================
      # 6. Buildozer Test
      # =====================================================
      - name: Test Buildozer
        shell: bash
        run: |
          set -eux

          echo "=========================================="
          echo "BUILDOZER TEST"
          echo "=========================================="

          python -m pip show buildozer

          echo
          echo "Python module:"
          python -c "import buildozer; print(buildozer.__file__)"

          echo
          echo "Buildozer version:"
          python -m buildozer --version

          echo
          echo "Buildozer module works."


      # =====================================================
      # 7. Android SDK
      # =====================================================
      - name: Verify Android SDK
        shell: bash
        run: |
          set -eux

          SDK="/usr/local/lib/android/sdk"
          NDK="$SDK/ndk/27.3.13750724"

          echo "=========================================="
          echo "ANDROID SDK"
          echo "=========================================="

          test -d "$SDK"
          test -d "$SDK/platforms/android-35"
          test -d "$SDK/build-tools/35.0.0"
          test -x "$SDK/build-tools/35.0.0/aidl"

          echo
          echo "ANDROID_HOME:"
          echo "$SDK"

          echo
          echo "ANDROID NDK:"
          echo "$NDK"

          test -d "$NDK"

          echo
          echo "AIDL:"
          "$SDK/build-tools/35.0.0/aidl" --version || true

          echo
          echo "NDK:"
          "$NDK/ndk-build" --version | head -n 5


      # =====================================================
      # 8. Configure Android Environment
      # =====================================================
      - name: Configure Android Environment
        shell: bash
        run: |
          set -eux

          SDK="/usr/local/lib/android/sdk"
          NDK="$SDK/ndk/27.3.13750724"

          echo "=========================================="
          echo "ANDROID ENVIRONMENT"
          echo "=========================================="

          echo "ANDROID_HOME=$SDK" >> "$GITHUB_ENV"
          echo "ANDROID_SDK_ROOT=$SDK" >> "$GITHUB_ENV"

          echo "ANDROID_NDK_HOME=$NDK" >> "$GITHUB_ENV"
          echo "ANDROID_NDK_ROOT=$NDK" >> "$GITHUB_ENV"
          echo "ANDROID_NDK=$NDK" >> "$GITHUB_ENV"

          echo "$SDK/platform-tools" >> "$GITHUB_PATH"
          echo "$SDK/cmdline-tools/latest/bin" >> "$GITHUB_PATH"
          echo "$SDK/build-tools/35.0.0" >> "$GITHUB_PATH"

          echo
          echo "Environment configured."


      # =====================================================
      # 9. Final Android Environment Test
      # =====================================================
      - name: Final Android Environment Test
        shell: bash
        run: |
          set -eux

          echo "=========================================="
          echo "FINAL ANDROID TEST"
          echo "=========================================="

          echo "ANDROID_HOME=$ANDROID_HOME"
          echo "ANDROID_SDK_ROOT=$ANDROID_SDK_ROOT"
          echo "ANDROID_NDK_HOME=$ANDROID_NDK_HOME"

          echo
          echo "AIDL:"
          command -v aidl || true
          aidl --version || true

          echo
          echo "SDK:"
          test -d "$ANDROID_HOME"

          echo
          echo "NDK:"
          test -d "$ANDROID_NDK_HOME"

          echo
          echo "Android environment OK."


      # =====================================================
      # 10. Check sdkmanager
      # =====================================================
      - name: Check sdkmanager
        shell: bash
        run: |
          set -eux

          SDK="$ANDROID_HOME"

          echo "=========================================="
          echo "SDKMANAGER CHECK"
          echo "=========================================="

          SDKMANAGER=""

          if [ -x "$SDK/cmdline-tools/latest/bin/sdkmanager" ]; then
            SDKMANAGER="$SDK/cmdline-tools/latest/bin/sdkmanager"
          else
            SDKMANAGER=$(find "$SDK/cmdline-tools" \
              -type f \
              -name sdkmanager \
              -perm -111 \
              | head -n 1 || true)
          fi

          if [ -z "$SDKMANAGER" ]; then
            echo "ERROR: sdkmanager not found."

            echo
            echo "Searching SDK:"
            find "$SDK" -type f -name sdkmanager -print || true

            exit 1
          fi

          echo
          echo "sdkmanager found:"
          echo "$SDKMANAGER"

          echo
          echo "sdkmanager version:"
          "$SDKMANAGER" --version


      # =====================================================
      # 11. Check Buildozer Configuration File
      # =====================================================
      - name: Check Buildozer Configuration
        shell: bash
        run: |
          set -eux

          echo "=========================================="
          echo "BUILDOZER CONFIGURATION"
          echo "=========================================="

          test -f buildozer.spec

          echo
          echo "buildozer.spec found."

          echo
          echo "Important settings:"
          grep -E \
            '^(title|package.name|package.domain|source.include_exts|requirements|android.api|android.minapi|android.ndk|android.arch)' \
            buildozer.spec || true

          echo
          echo "Buildozer configuration check OK."


      # =====================================================
      # 12. Validate Buildozer Configuration
      # =====================================================
      - name: Validate Buildozer Configuration
        shell: bash
        run: |
          set -euxo pipefail

          echo "=========================================="
          echo "BUILDOZER CONFIGURATION"
          echo "=========================================="

          echo
          echo "Important Buildozer settings:"

          grep -E \
            '^(title|package.name|package.domain|source.include_exts|requirements|android.api|android.minapi|android.ndk|android.arch)' \
            buildozer.spec || true

          echo
          echo "Buildozer configuration detected."
      
      # =====================================================
      # 13. Verify Android SDK / NDK
      # =====================================================
      - name: Verify Android SDK and NDK
        shell: bash
        run: |
          set -euxo pipefail

          echo "=========================================="
          echo "VERIFY ANDROID SDK / NDK"
          echo "=========================================="

          SDK="/usr/local/lib/android/sdk"
          NDK="$SDK/ndk/27.3.13750724"

          echo
          echo "ANDROID SDK:"
          echo "$SDK"

          echo
          echo "ANDROID NDK:"
          echo "$NDK"

          test -d "$SDK"
          test -d "$NDK"

          echo
          echo "Checking NDK source.properties:"
          test -f "$NDK/source.properties"
          cat "$NDK/source.properties"

          echo
          echo "Checking ndk-build:"
          test -x "$NDK/ndk-build"

          "$NDK/ndk-build" --version | head -n 5

          echo
          echo "SDK/NDK verification PASSED."


      # =====================================================
      # 14. Configure Android Environment
      # =====================================================
      - name: Configure Android Environment
        shell: bash
        run: |
          set -euxo pipefail

          SDK="/usr/local/lib/android/sdk"
          NDK="$SDK/ndk/27.3.13750724"

          echo "=========================================="
          echo "CONFIGURE ANDROID ENVIRONMENT"
          echo "=========================================="

          echo "ANDROID_HOME=$SDK" >> "$GITHUB_ENV"
          echo "ANDROID_SDK_ROOT=$SDK" >> "$GITHUB_ENV"

          echo "ANDROID_NDK_HOME=$NDK" >> "$GITHUB_ENV"
          echo "ANDROID_NDK_ROOT=$NDK" >> "$GITHUB_ENV"
          echo "ANDROID_NDK=$NDK" >> "$GITHUB_ENV"

          echo "$SDK/platform-tools" >> "$GITHUB_PATH"
          echo "$SDK/build-tools/35.0.0" >> "$GITHUB_PATH"

          if [ -d "$SDK/cmdline-tools/latest/bin" ]; then
            echo "$SDK/cmdline-tools/latest/bin" >> "$GITHUB_PATH"
          fi

          echo
          echo "Android environment configured."


      # =====================================================
      # 15. Verify Buildozer Spec
      # =====================================================
      - name: Verify Buildozer Spec
        shell: bash
        run: |
          set -euxo pipefail

          echo "=========================================="
          echo "VERIFY BUILDOZER SPEC"
          echo "=========================================="

          test -f buildozer.spec

          echo
          echo "Android settings:"
          grep -E \
            '^(android.api|android.minapi|android.ndk|android.ndk_path|android.sdk_path|android.ndk_api|android.skip_update|android.accept_sdk_license)' \
            buildozer.spec

          echo
          echo "Buildozer spec verified."


      # =====================================================
      # 16. Verify Exact Build Paths
      # =====================================================
      - name: Verify Exact Build Paths
        shell: bash
        run: |
          set -euxo pipefail

          SDK="/usr/local/lib/android/sdk"
          NDK="$SDK/ndk/27.3.13750724"

          echo "=========================================="
          echo "EXACT BUILD PATH CHECK"
          echo "=========================================="

          echo
          echo "SDK:"
          realpath "$SDK"

          echo
          echo "NDK:"
          realpath "$NDK"

          echo
          echo "ANDROID_HOME:"
          echo "$ANDROID_HOME"

          echo
          echo "ANDROID_NDK_HOME:"
          echo "$ANDROID_NDK_HOME"

          test "$ANDROID_HOME" = "$SDK"
          test "$ANDROID_NDK_HOME" = "$NDK"

          echo
          echo "Exact paths OK."


      # =====================================================
      # 17. Buildozer Test
      # =====================================================
      - name: Final Buildozer Test
        shell: bash
        run: |
          set -euxo pipefail

          echo "=========================================="
          echo "FINAL BUILDOZER TEST"
          echo "=========================================="

          python --version

          python -m buildozer --version

          python -c \
            "import buildozer; print(buildozer.__file__)"

          echo
          echo "Buildozer test PASSED."


      # =====================================================
      # 18. Clean Previous Failed Build
      # =====================================================
      - name: Clean Previous Build
        shell: bash
        run: |
          set -euxo pipefail

          echo "=========================================="
          echo "CLEAN PREVIOUS BUILDOZER STATE"
          echo "=========================================="

          rm -rf .buildozer

          mkdir -p bin

          echo
          echo "Old Buildozer state removed."


      # =====================================================
      # 19. Re-Verify Environment After Clean
      # =====================================================
      - name: Re-Verify Android Environment
        shell: bash
        run: |
          set -euxo pipefail

          SDK="/usr/local/lib/android/sdk"
          NDK="$SDK/ndk/27.3.13750724"

          echo "=========================================="
          echo "RE-VERIFY ANDROID ENVIRONMENT"
          echo "=========================================="

          test -d "$SDK"
          test -d "$NDK"

          echo
          echo "SDK:"
          ls -ld "$SDK"

          echo
          echo "NDK:"
          ls -ld "$NDK"

          echo
          echo "Environment ready for Buildozer."

      # =====================================================
      # 20A. Fix SDK Manager Compatibility
      # =====================================================
      - name: Fix SDK Manager Compatibility
        shell: bash
        run: |
          set -euxo pipefail

          echo "=========================================="
          echo "FIXING SDKMANAGER"
          echo "=========================================="

          SDK="/usr/local/lib/android/sdk"

          echo
          echo "Searching for sdkmanager..."

          find "$SDK" \
            -type f \
            -name "sdkmanager" \
            -print

          SDKMANAGER=""

          if [ -x "$SDK/cmdline-tools/latest/bin/sdkmanager" ]; then
            SDKMANAGER="$SDK/cmdline-tools/latest/bin/sdkmanager"
          elif [ -x "$SDK/cmdline-tools/bin/sdkmanager" ]; then
            SDKMANAGER="$SDK/cmdline-tools/bin/sdkmanager"
          else
            SDKMANAGER=$(find "$SDK/cmdline-tools" \
              -type f \
              -name "sdkmanager" \
              -perm -111 \
              | head -n 1 || true)
          fi

          if [ -z "$SDKMANAGER" ]; then
            echo "ERROR: sdkmanager was not found."
            exit 1
          fi

          echo
          echo "Found sdkmanager:"
          echo "$SDKMANAGER"

          echo
          echo "sdkmanager version:"
          "$SDKMANAGER" --version

          echo
          echo "Creating compatibility path..."

          mkdir -p "$SDK/tools/bin"

          ln -sf "$SDKMANAGER" "$SDK/tools/bin/sdkmanager"

          echo
          echo "Compatibility path:"
          ls -l "$SDK/tools/bin/sdkmanager"

          echo
          echo "Testing compatibility path..."

          "$SDK/tools/bin/sdkmanager" --version

          echo
          echo "Adding SDK tools to PATH..."

          echo "$SDK/tools/bin" >> "$GITHUB_PATH"
          echo "$SDK/cmdline-tools/latest/bin" >> "$GITHUB_PATH"

          echo
          echo "SDK Manager compatibility FIXED."


      # =====================================================
      # 20. Build APK
      # =====================================================
      - name: Build V16 Antivirus APK
        shell: bash
        run: |
          set -euxo pipefail

          echo "=========================================="
          echo "STARTING APK BUILD"
          echo "=========================================="

          echo
          echo "Python:"
          python --version

          echo
          echo "Buildozer:"
          python -m buildozer --version

          echo
          echo "ANDROID_HOME:"
          echo "$ANDROID_HOME"

          echo
          echo "ANDROID_NDK_HOME:"
          echo "$ANDROID_NDK_HOME"

          echo
          echo "SDK:"
          ls -ld "$ANDROID_HOME"

          echo
          echo "NDK:"
          ls -ld "$ANDROID_NDK_HOME"

          echo
          echo "=========================================="
          echo "BUILDOZER BUILD"
          echo "=========================================="

          python -m buildozer -v android debug


      # =====================================================
      # 21. Verify APK Output
      # =====================================================
      - name: Verify APK Output
        if: success()
        shell: bash
        run: |
          set -euxo pipefail

          echo "=========================================="
          echo "VERIFYING APK OUTPUT"
          echo "=========================================="

          echo
          echo "Searching for APK files..."

          APK_COUNT=$(find . \
            -type f \
            -name "*.apk" \
            ! -path "./.git/*" \
            | wc -l)

          echo
          echo "APK count: $APK_COUNT"

          if [ "$APK_COUNT" -eq 0 ]; then
            echo "ERROR: No APK file was generated."
            exit 1
          fi

          echo
          echo "APK files found:"

          find . \
            -type f \
            -name "*.apk" \
            ! -path "./.git/*" \
            -print

          echo
          echo "APK output verified."


      # =====================================================
      # 22. Show APK Details
      # =====================================================
      - name: Show APK Details
        if: success()
        shell: bash
        run: |
          set -euxo pipefail

          echo "=========================================="
          echo "APK DETAILS"
          echo "=========================================="

          find . \
            -type f \
            -name "*.apk" \
            ! -path "./.git/*" \
            -print0 |
          while IFS= read -r -d '' APK; do

            echo
            echo "------------------------------------------"
            echo "APK:"
            echo "$APK"

            echo
            echo "File size:"
            du -h "$APK"

            echo
            echo "SHA256:"
            sha256sum "$APK"

          done

          echo
          echo "APK details completed."


      # =====================================================
      # 23. Find APK and AAB
      # =====================================================
      - name: Find APK and AAB
        if: always()
        shell: bash
        run: |
          set +e

          echo "=========================================="
          echo "APK / AAB FILES"
          echo "=========================================="

          echo
          echo "APK files:"

          find . \
            -type f \
            -name "*.apk" \
            ! -path "./.git/*" \
            -print

          echo
          echo "AAB files:"

          find . \
            -type f \
            -name "*.aab" \
            ! -path "./.git/*" \
            -print


      # =====================================================
      # 24. Upload APK
      # =====================================================
      - name: Upload APK
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: V16-Antivirus-APK
          path: |
            bin/*.apk
            bin/*.aab
          if-no-files-found: error


      # =====================================================
      # 25. Upload Build Logs
      # =====================================================
      - name: Upload Build Logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: V16-Buildozer-Logs
          path: |
            .buildozer/android/platform/python-for-android/
            .buildozer/android/platform/build-*/build/
            .buildozer/android/platform/*/build/
            .buildozer/android/platform/android/
            buildozer.spec
          if-no-files-found: warn
